export class Carro {
    modelo: string;
    ano: number;
    marca: string;
    velocidade: number;

    constructor(modelo:string, ano:number, marca:string, velocidade:number) {
        this.modelo = modelo;
        this.ano = ano;
        this.marca = marca;
        this.velocidade = velocidade
    }
    nome():void {
        console.log(`${this.modelo}, O modelo é`)
     }
      anoDeFabricacao():void {
        console.log (`${this.ano}, Ano de fabricação`)
     }
     fabricante():void {
        console.log(`${this.fabricante}, O fabricante`)
     }
     kmPorHoras():void {
        console.log(`${this.velocidade}, O fabricante`)
}
}

const newCarro:Carro = new Carro('Palio', 2013, 'FIAT', 80)
console.log(newCarro.modelo)

newCarro.velocidade()


