//Crie uma interface chamada produtos que tenha os atributos tipo, preco, nome, peso
//Crie pelo menos um método obrigatório nesta interface

// Crie 3 classes que utilizem esta interface. Crie um método diferente para cada produto.

// Instancie as classes no index.ts e chame estes métodos e atributos.

import { Comida } from 'Exercícios/Comida';
import { Carro } from 'Herança_encapsulamento/Carro';
import { Moto } from 'Herança_encapsulamento/Moto';

interface Produtos {
    
    preco:number
    nome:string
    peso:number
    starEngine():void
}

