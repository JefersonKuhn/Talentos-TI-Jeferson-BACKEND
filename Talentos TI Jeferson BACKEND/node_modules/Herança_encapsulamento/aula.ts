/*export classe person{
    protected children: number;

    constructor(children: number) {
        this.children = children;
    }
    protected howmanychildren(): void {
        console.log(`this person has ${this.childen} children. `);
    }
}

import { person} from "./person";
class father extends person {
    falar(): void {
        console.log("this is the father.");
        this.howmanychildren;  // Pode acessar membros protected da classe pai
    }
}

const pai = new person(4);
pai.howmanychildren(); // Não pode acessar fora da classe

const novoPai: father = new father(3)
novoPai.falar();


// public - pode ser chamado de qualquer lugar
// protected - pode ser chamado somente dentro da classe filha
// private - pode ser chamado somente dentro da classe pai*/
