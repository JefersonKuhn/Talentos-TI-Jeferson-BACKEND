// Faça um contador que vá de 10 até o usando "for"
// A cada 'segundo' ou melhor, a cada iteração, o código deve verificar se valor de "i" é par ou impar.
// se for par, imprime no console a palavra 'tic'
// Se for impar, imprime no console a palavra 'tac'
// Quando chegar a zero, imprime no console a palavra 'BOOOOM'.

/*for (let i:number = 10; i > 0; i--) {
    if(i % 2 === 0) {
        console.log('Boooom')
    } else if (i % 1 === 0) {
        console.log('tic')
    } else if (i % 2 === 0) {
        console.log('tac')
    }
}


for (let i:string = 'a'; i !== 'abababa'; i +='aba') {
    console.log(i)
}

let ficcao = {
    "Fundação": "Issac asimov",
    "duna": "Frank Herbert"
}

for (let Livro in ficcao) {
        console.log(`Autor do livro"${Livro}": ${ficcao [Livro]}`);
}


let meuObjeto:object = {
    nome1: 'valor1'
    nome2: 'valor2'
    nome3: 'valor3'
    nome4: 'valor4'
    nome5: 'valor5'
    
}
for (let elemento in meuObjeto){
    console.log (`O elemento ${elemento}tem valor ${meuObjeto[elemento]}`)
}


// Array representando "For of"
let meuslivros =["Fundação", "Duna", "Orgulho e Preconceito"];

for (let livro of meuslivros){
    console.log(livro);
}


let listaDeFrutas =['Pera','Maçã',"Maracuja"];

for(let fruta of listaDeFrutas) {
    console.log(fruta)
}


let tolkienLivros =['Senhor dos Aneis', 'O Hobbit', 'Contos na Cabana'];

tolkienLivros.forEach(Livro = > {
    console.log(`eu já li ${Livro}`),
})


const arr =     [ "Carro", "moto", "Bicicleta"];

arr.forEach((Element, index, Array) => {
    console.log (`Elemento ${Element} no indice ${index}`);
    console.log ('Array original:',Array);
});*/